# The URL the site will be built for
base_url = "https://lcdsmao.dev"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

theme = "terminimal"

[extra]
# One of: blue, green, orange, pink, red.
# Defaults to blue. 
accent_color = "my-color"

# One of: blue, dark, green, orange, pink, red.
# Enabling dark background will also modify primary font color
# to be darker.
# Defaults to accent color (or, if not accent color specified, to blue). 
background_color = ""

# The logo text - defaults to "Terminimal theme"
logo_text = "lcdsmao"

# Author name: when specified, modifies the default
# copyright text. Apart from author, it will
# contain current year and a link to the theme.
author = "lcdsmao"

# The text shown at the bottom of a post,
# before earlier/later post links.
# Defaults to "Thanks for reading! Read other posts?"
post_view_navigation_prompt = "Read more"

# menu is enabled by adding menu_items (optional)
menu_items = [
    # each of these is optional, name and url are required
    # $BASE_URL is going to be substituted by base_url from configuration
    # {name = "Home", url = "$BASE_URL"},
    
    # tags should only be enabled if you have "tags" taxonomy
    # see documentation below for more details
    # {name = "tags", url = "$BASE_URL/tags"},
    {name = "Blog", url = "$BASE_URL/archive"},
    
    # set newtab to true to make the link open in new tab
    {name = "Github", url = "https://github.com/lcdsmao", newtab = true},
    {name = "Twitter", url = "https://twitter.com/lcdsmao", newtab = true},

    {name = "About", url = "$BASE_URL/about"},
]
